{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run fixture",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/northstardtl",
      "args": ["run", "${workspaceFolder}/tests/fixtures/settlement.json"]
    }
  ]
}
